

LLM_DEFAULT_MODEL=gpt-5
VL_MODEL=gpt-5
OPENAI_API_KEY=
OPENAI_BASE_URL=
LLM_DEFAULT_TEMPERATURE=0.1
COMPRESSION_MODEL=gpt-4


#MINERU
MINERU_API_TOKEN=
MINERU_BASE_URL=https://mineru.net/api/v4
MINERU_MODEL_SOURCE=modelscope
MODELSCOPE_OFFLINE=1
MODELSCOPE_CACHE=./asset/models/modelscope_cache
HF_HOME=./asset/models/huggingface_cache
MD_ROOT_DIR=./temp-files/


# 阿里云mcp配置
ALIYUN_API_KEY=
MCP_DASHSCOPE_BASE_URL_STREAMABLE=
ALIYUN_API_BASE_URL=


# embedding模型配置
BGE_M3_PATH=
BGE_M3=BAAI/bge-m3
BGE_DEVICE=cpu
BGE_FP16=0
ITEM_NAME_DIAG=1


# Milvus 配置
MILVUS_URL=
# 存储切片集合
CHUNKS_COLLECTION=meeting_chunks
# 存储每个文档对应的实体类
CONVERSATION_MEMORY_COLLECTION=meeting_conversation_memory
OFFICE_TASK_COLLECTION=office_tasks
# 存储文档元数据的集合名称
DOC_META_COLLECTION=meeting_doc_meta
EMBEDDING_DIM=1024
MILVUS_TOKEN=""


#Mongodb的配置
# 密码@需要转义，否则会报错
MONGO_URL=
MONGO_DB_NAME=hiyi

MONGO_MEETING_MEMORY_COLLECTION=meeting_episodes
MONGO_PROFILE_ATTRIBUTE_COLLECTION=profile_attributes



ASR_PROVIDER=funasr_streaming
ASR_MODEL=paraformer-zh-streaming
ASR_PUNC_MODEL=ct-punc
ASR_DEVICE=cpu
ASR_CHUNK_SIZE=5,10,5
ASR_ENCODER_CHUNK_LOOK_BACK=4
ASR_DECODER_CHUNK_LOOK_BACK=1
ASR_SILENCE_RMS=0.008
ASR_ENDPOINT_SILENCE_MS=800
# 仅在文本达到该长度且检测到句末时切分；不会按字数强制截断。
ASR_SENTENCE_SPLIT_THRESHOLD=80

MONGO_SERVER_SELECTION_TIMEOUT_MS=5000
MONGO_CONNECT_TIMEOUT_MS=5000
MONGO_MAX_POOL_SIZE=20


#minio客户端
MINIO_ENDPOINT=
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
MINIO_BUCKET_NAME=meeting-files
MINIO_IMG_DIR=upload-images
MINIO_DOC_DIR=upload-docs
MINIO_SECURE=False

# mysql配置
# 密码@需要转义，否则会报错
MYSQL_URL=
MYSQL_POOL_SIZE=100
MYSQL_MAX_OVERFLOW=206
MYSQL_POOL_TIMEOUT=10.0
MYSQL_POOL_RECYCLE=1800
MYSQL_ECHO_SQL=False



# 重排序模型配置
BGE_RERANKER_LARGE=
#BGE_RERANKER_DEVICE=cuda:0
BGE_RERANKER_DEVICE=cpu
#BGE_RERANKER_FP16=1
BGE_RERANKER_FP16=0


# Redis 配置
REDIS_HOST=
REDIS_PORT=
REDIS_DB=0
# 是否开启记忆缓存
REDIS_MEMORY_CACHE_ENABLED=true
# 记忆缓存键前缀
REDIS_MEMORY_CACHE_PREFIX=hviyi
# Redis 连接超时时间（秒）
REDIS_CONNECT_TIMEOUT=1.0
# Redis 单次请求超时时间（秒）
REDIS_SOCKET_TIMEOUT=1.0

REDIS_MEMORY_L1_TTL=1800
REDIS_MEMORY_L2_TTL=3600
REDIS_MEMORY_L4_TTL=600



# ===================== 日志配置 =====================
# 控制台日志：True=开启/False=关闭，级别可选(DEBUG/INFO/WARNING/ERROR/CRITICAL)
LOG_CONSOLE_ENABLE=True
LOG_CONSOLE_LEVEL=INFO

# 日志配置
# 文件日志：True=开启/False=关闭，级别与控制台可独立设置
LOG_FILE_ENABLE=True
LOG_FILE_LEVEL=INFO
# 日志文件保留天数（自动删除过期日志，避免磁盘占满）
LOG_FILE_RETENTION=7 days
